import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vyronixcoder.smashlearn',
  appName: 'Smash Learn',
  webDir: 'dist'
};

export default config;
